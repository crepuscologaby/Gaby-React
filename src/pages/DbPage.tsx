// Pagina segnaposto (placeholder) per la sezione DB.
// Qui in futuro mostreremo i dati che arrivano dal database SQL (Supabase).

export default function DbPage() {
  return (
    <section className="min-h-screen px-8 pt-20 flex flex-col items-center justify-center text-center">
      <h1 className="text-4xl font-bold mb-4">Sezione DB</h1>
      <p className="text-gray-600">
        Qui mostreremo i dati recuperati dal database SQL. (da sviluppare)
      </p>
    </section>
  );
}
